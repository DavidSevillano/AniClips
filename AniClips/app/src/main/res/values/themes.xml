<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AniClips" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/backgroud_app</item> <!-- Asigna el color deseado aquí -->

    </style>

    <style name="Theme.AniClips" parent="Base.Theme.AniClips" />

    <style name="Theme.AniClips.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AniClips.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AniClips.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
